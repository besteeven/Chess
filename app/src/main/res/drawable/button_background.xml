<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/bordeaux" /> <!-- Rouge bordeaux -->
    <stroke android:width="2dp" android:color="#000000" /> <!-- Contour noir -->
    <corners android:radius="16dp" />
</shape>
